# OpenAI API Key (required for full functionality)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (for Claude models)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Tavily API Key (optional — enables cheaper dedicated web search)
# Get your key at: https://tavily.com/
# TAVILY_API_KEY=your_tavily_api_key_here

# Server configuration
HOST=0.0.0.0
PORT=3000

# Database configuration
# For production: Set DATABASE_URL to your PostgreSQL connection string
# DATABASE_URL

# For local development: Leave DATABASE_URL unset (uses SQLite automatically)
# Or explicitly set USE_SQLITE=true to force SQLite even if DATABASE_URL is set
# USE_SQLITE=true

# Security configuration
# JWT secret for token signing (REQUIRED in production)
JWT_SECRET=your-secret-key-change-in-production

# Encryption key for AES-256-GCM (optional, derived from JWT_SECRET if not set)
# ENCRYPTION_KEY=your-32-byte-encryption-key

# HMAC secret for audit log signatures (optional, uses JWT_SECRET if not set)
# HMAC_SECRET=your-hmac-secret

# CSRF secret (optional, uses JWT_SECRET if not set)
# CSRF_SECRET=your-csrf-secret

# Environment (set to "production" in production)
# ENVIRONMENT=production

# CORS allowed origins (comma-separated, defaults to localhost for dev)
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Redis URL for rate limiting and caching (optional, uses in-memory if not set)
# REDIS_URL=redis://localhost:6379

# Critical security event webhook (optional)
# CRITICAL_ALERT_WEBHOOK=https://your-webhook-url.com/security-alerts

# Audit log retention in days (default: 365)
# AUDIT_RETENTION_DAYS=365

# Max request body size in bytes (default: 10MB)
# MAX_REQUEST_BODY_SIZE=10485760
