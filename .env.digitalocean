# DigitalOcean App Platform Environment Variables
# Upload this file in: App → Settings → Web Service Component → Environment Variables → Upload .env

# =============================================================================
# REQUIRED — fill these in before uploading
# =============================================================================

# Database (DigitalOcean Managed PostgreSQL connection string)
DATABASE_URL=

# Redis/Valkey (DigitalOcean Managed Valkey connection string)
REDIS_URL=

# API Keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# JWT secret for auth tokens — generate one with: openssl rand -hex 32
JWT_SECRET=

# =============================================================================
# REQUIRED — set but don't change
# =============================================================================

ENVIRONMENT=production
HOST=0.0.0.0
PORT=8000

# =============================================================================
# OPTIONAL — uncomment and set if needed
# =============================================================================

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Encryption key for AES-256-GCM (derived from JWT_SECRET if not set)
# ENCRYPTION_KEY=

# HMAC secret for audit log signatures (uses JWT_SECRET if not set)
# HMAC_SECRET=

# CSRF secret (uses JWT_SECRET if not set)
# CSRF_SECRET=

# Audit log retention in days (default: 365)
# AUDIT_RETENTION_DAYS=365

# Max request body size in bytes (default: 10MB)
# MAX_REQUEST_BODY_SIZE=10485760

# Critical security event webhook
# CRITICAL_ALERT_WEBHOOK=
